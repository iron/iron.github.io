(function() {var implementors = {};
implementors['lib'] = ["<a class='stability Unmarked' title='No stability level'></a>impl Key for <a class='struct' href='lib/struct.Router.html' title='lib::Router'>Router</a>",];

            if (window.register_implementors) {
                window.register_implementors(implementors);
            } else {
                window.pending_implementors = implementors;
            }
        
})()
