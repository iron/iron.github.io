initSidebarItems({"fn":[["encode_integer","Encode an integer to the representation defined by HPACK.Returns a newly allocated `Vec` containing the encoded bytes. Only `prefix_size` lowest-order bits of the first byte in the array are guaranteed to be used."]],"struct":[["Encoder","Represents an HPACK encoder. Allows clients to encode arbitrary header sets and tracks the encoding context. That is, encoding subsequent header sets will use the context built by previous encode calls.This is the main API for performing HPACK encoding of headers.ExamplesEncoding a header two times in a row produces two different representations, due to the utilization of HPACK compression."]]});