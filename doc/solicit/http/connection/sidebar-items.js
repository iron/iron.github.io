initSidebarItems({"enum":[["EndStream","An enum indicating whether the `HttpConnection` send operation should end the stream."],["HttpFrame","An enum representing all frame variants that can be returned by an `HttpConnection`.The variants wrap the appropriate `Frame` implementation."],["SendStatus","The enum represents the success status of the operation of sending a next data chunk on an HTTP/2 connection."]],"struct":[["DataChunk","The struct represents a chunk of data that should be sent to the peer on a particular stream."],["HttpConnection","The struct implements the HTTP/2 connection level logic.This means that the struct is a bridge between the low level raw frame reads/writes (i.e. what the `SendFrame` and `ReceiveFrame` traits do) and the higher session-level logic.Therefore, it provides an API that exposes higher-level write operations, such as writing headers or data, that take care of all the underlying frame construction that is required.Similarly, it provides an API for handling events that arise from receiving frames, without requiring the higher level to directly look at the frames themselves, rather only the semantic content within the frames."]],"trait":[["ReceiveFrame","A trait that should be implemented by types that can provide the functionality of receiving HTTP/2 frames."],["SendFrame","A trait that should be implemented by types that can provide the functionality of sending HTTP/2 frames."]]});