initSidebarItems({"struct":[["Iron","The primary entrance point to `Iron`, a `struct` to instantiate a new server.`Iron` contains the `Handler` which takes a `Request` and produces a `Response`."]],"trait":[["Pluggable","An interface for plugins that cache values between calls.`R` is the type of the plugin's return value, which must be cloneable."],["Set","A trait providing the set and set_mut methods for all types.Simply implement this for your types and they can be used with modifiers."]]});